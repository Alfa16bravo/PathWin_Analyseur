<ui:FluentWindow x:Class="PathAnalyzer.MessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Width="500" SizeToContent="Height" ResizeMode="NoResize" MinHeight="0" MinWidth="0"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica" WindowCornerPreference="Round"
        FontSize="13">
    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" x:Name="Bar" ShowMaximize="False" ShowMinimize="False">
            <ui:TitleBar.Icon>
                <ui:ImageIcon Source="pack://application:,,,/Assets/app.ico" Width="16" Height="16" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <!-- Corps -->
        <Grid Grid.Row="1" Margin="24,4,24,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border x:Name="Pill" Grid.Column="0" Width="44" Height="44" CornerRadius="22"
                    VerticalAlignment="Top" Margin="0,2,16,0">
                <ui:SymbolIcon x:Name="Glyph" Filled="True" FontSize="22"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>

            <StackPanel Grid.Column="1">
                <TextBlock x:Name="HeadingText" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap"
                           Margin="0,0,0,6" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" LineHeight="19"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <!-- Liste détaillée (chemins concernés par l'action) -->
                <Border x:Name="DetailsBox" Visibility="Collapsed" Margin="0,14,0,0" CornerRadius="8"
                        Background="{DynamicResource SubtleFillColorSecondaryBrush}" Padding="12,10">
                    <ScrollViewer MaxHeight="240" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBox x:Name="DetailsText" Style="{StaticResource MonoText}" TextWrapping="NoWrap" />
                    </ScrollViewer>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Boutons -->
        <Border Grid.Row="2" Background="Transparent"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="0,1,0,0"
                Padding="24,14">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button x:Name="PrimaryButton" Style="{StaticResource PrimaryButton}" MinWidth="110"
                           Click="Primary_Click" IsDefault="True" Margin="0,0,8,0" />
                <ui:Button x:Name="SecondaryButton" Style="{StaticResource ToolButton}" MinWidth="110"
                           Click="Secondary_Click" IsCancel="True" Margin="0" />
            </StackPanel>
        </Border>
    </Grid>
</ui:FluentWindow>
